import React from 'react';

const ROption = ({ value, children }) => {
  // This component doesn't render anything directly.
  // Its purpose is to pass its props (value and children) to the parent CustomSelect.
  return null;
};

export default ROption;